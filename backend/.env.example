# Database Configuration
# Format: postgresql://username:password@host:port/database
# For local PostgreSQL without password (common setup):
DATABASE_URL=postgresql://postgres@localhost/globemap
# Or if you have a different username:
# DATABASE_URL=postgresql://your_username@localhost/globemap
# Or with password:
# DATABASE_URL=postgresql://username:password@localhost/globemap

# External API Keys (Optional - seed script will skip if not provided)
EVERYORG_API_KEY=pk_live_76b861b8d71a5cccd09c81c340021d71
GLOBALGIVING_API_KEY=

# Note: ReliefWeb and USGS don't require API keys and work out of the box
# OpenCollective charity integration will be skipped if APIs fail (graceful degradation)
